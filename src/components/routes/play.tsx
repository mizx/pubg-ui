import React from 'react';

import Typography from 'components/typography';

export const PlayComponent: React.SFC = props => (
  <div>
    <Typography type="main">Let's play!</Typography>
  </div>
);

export default PlayComponent;
